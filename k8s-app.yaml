apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: "nginx"
spec:
  descriptor:
    links:
      - description: Open The Application
        # needs to match url in kots-app.yaml
        url: "http://nginx"